-- x send 16.16 bit values from javascript instead of uint8
-- x multadd scaling node seems to be weird with sine wave
-- x add debug node
-- x split files
-- x emit control values on creation
-- x save graph to local storage
-- x socket colors
-- x download local graph
-- x save json graph to reload later on
-- x upload local graph

-- x fix setting negative numbers
-- x add generic particle node
-- x add circular emitter
-- add duplicate node functionality
-- add linear emitter
-- add clear button
-- add "recent projects"
-- add function sockets for mapping
-- better control and input node values
-- add submodules
-- save to local storage with a name
-- add function sockets for mapping
-- gate modules
-- comparator modules
-- add lua generation for the graph so that this can be debugged in the console
-- add flag input controls
